<UserControl x:Class="BlogSystemClient.Views.LoginRegisterForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="Username" Target="Username"></Label>
        <TextBox Grid.Row="0" Grid.Column="1" Name="Username"></TextBox>

        <Label Grid.Row="1" Grid.Column="0" Content="Password" Target="Password"></Label>
        <TextBox Grid.Row="1" Grid.Column="1" Name="Password"></TextBox>
        
        <Button Grid.Row="2" Grid.Column="0" Command="{Binding Login}">Login</Button>
        <Button Grid.Row="2" Grid.Column="1" Command="{Binding Register}">Register</Button>
    </Grid>
</UserControl>
